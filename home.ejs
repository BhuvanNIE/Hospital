<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f8ff;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #333;
        }
        h1 {
            font-size: 2.5em;
            color: #2c3e50;
        }
        form {
            margin-top: 20px;
        }
        button {
            background-color: #3498db;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        button:hover {
            background-color: #2980b9;
        }
        center {
            background-color: #fff;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body>
    <center>
        <h1>Hospital Dashboard</h1>
        <form action="/entryp" method="get">
            <button type="submit">Patient Entry</button>
        </form>
        <form action="/entryd" method="get">
            <button type="submit">Doctor Entry</button>
        </form>
        <form action="/pinfo" method="get">
            <button type="submit">Patient Details</button>
        </form>
        <form action="/dinfo" method="get">
            <button type="submit">Doctor Details</button>
        </form>
        <form action="/dpinfo" method="get">
            <button type="submit">Patient:DOC-ID</button>
        </form>
    </center>
</body>
</html>
